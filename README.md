# ESLint Configuration File for MSA

For your project:
* install eslint and our config
  * ```npm install eslint @msa/eslint-config```
  * or
  * ```yarn add eslint @msa/eslint-config```
* Add .eslintrc.yaml file
  * 
```yaml
extends: '@msa'
```
